version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-video-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    networks:
      - bot-network
    mem_limit: 1g
    cpus: 1

networks:
  bot-network:
    driver: bridge
